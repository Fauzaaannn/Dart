void main(List<String> args) {
  
  String hello = "hello";
  const String world = "world"; // once we flag variabel as a constant, it can never change - definition of const
  // const is a scientific term, it means that it is a constant value, that will remain the same.
  
  // world = 'Fauzan';

  print(hello + " " + world);

}