import 'package:polymorphism/monster.dart';

void main(List<String> args) {
  Monster ugly = new Monster();

  ugly.test();
  // ugly.
}

// true multiple inheritance you could actually have that test function in all those other classes, 
// but through the mixin it gets a little more complicated